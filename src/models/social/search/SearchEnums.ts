/**
 * Search Result Types.
 */
export enum SearchType {
  USER = 'USER',
  GROUP = 'GROUP',
  POST = 'POST',
  GAME = 'GAME',
  NEWS = 'NEWS'
}

/**
 * Categories for search.
 */
export enum SearchCategory {
  ALL = 'all',
  USERS = 'users',
  GROUPS = 'groups',
  POSTS = 'posts',
  FORUMS = 'forums',
  NEWS = 'news'
}
